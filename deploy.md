# deploy.md — دليل النشر خطوة بخطوة

هذا الدليل يفترض أنك بدأت من مجلد المشروع كما استلمته، ولا يحتاج أي خبرة سابقة بـ Git.

---

## الخطوة 1 — تجهيز حساب GitHub

1. إن لم يكن لديك حساب، أنشئ واحداً مجاناً على https://github.com
2. سجّل الدخول.

---

## الخطوة 2 — إنشاء مستودع (Repository) جديد

1. من الزاوية العلوية اليمنى، اضغط **+** ثم **New repository**.
2. اختر اسماً (مثال: `dabaa-plan`).
3. اجعله **Public** (مطلوب لتفعيل GitHub Pages مجاناً في الحسابات المجانية).
4. **لا تُفعّل** خيار "Add a README file" (المشروع يحتوي بالفعل على README.md خاص به).
5. اضغط **Create repository**.

---

## الخطوة 3 — رفع المشروع (من جهازك)

افتح Terminal / موجه الأوامر داخل مجلد المشروع نفسه، ونفّذ بالترتيب:

```bash
git init
git add .
git commit -m "Initial commit - خطة موجهي الضبعة"
git branch -M main
git remote add origin https://github.com/USERNAME/REPO_NAME.git
git push -u origin main
```

استبدل `USERNAME` باسم مستخدمك على GitHub، و`REPO_NAME` باسم المستودع الذي أنشأته.

> إذا طُلب منك تسجيل دخول: استخدم اسم المستخدم، وككلمة مرور استخدم **Personal Access Token** (وليس كلمة مرور حسابك العادية) — يمكن إنشاؤه من: `Settings → Developer settings → Personal access tokens`.

---

## الخطوة 4 — تفعيل GitHub Pages

1. من صفحة المستودع على GitHub، اذهب إلى **Settings**.
2. من القائمة الجانبية، اختر **Pages**.
3. تحت **Build and deployment → Source**، اختر **Deploy from a branch**.
4. تحت **Branch**، اختر **main** و**/ (root)**، ثم اضغط **Save**.
5. انتظر دقيقة أو دقيقتين، ثم أعد تحميل الصفحة — سيظهر رابط أخضر بالشكل:
   `https://USERNAME.github.io/REPO_NAME/`
6. افتح الرابط وتأكد أن التطبيق يعمل بشكل صحيح.

---

## الخطوة 5 — اختبار PWA على الرابط المنشور

1. افتح الرابط على جوال أو حاسوب عبر Chrome.
2. تأكد من ظهور زر "تثبيت التطبيق" في الشريط العلوي (يظهر فقط إذا كان التثبيت متاحاً فعلياً حسب المتصفح).
3. ثبّت التطبيق، ثم افتح إعدادات الشبكة وفعّل وضع الطيران، وأعد فتح التطبيق المثبَّت — يجب أن يستمر بالعمل ويعرض آخر بيانات محفوظة.

---

## الخطوة 6 — ربط Google Sheets (اختياري)

راجع القسم **"3) ربط Google Sheets"** في `README.md` لخطوات إنشاء ونشر `apps-script/Code.gs`. بعد الحصول على رابط الـ Web App:

1. عدّل `js/config.js` محلياً وضع الرابط في `SCRIPT_URL`.
2. ارفع التعديل:
   ```bash
   git add js/config.js
   git commit -m "ربط Google Sheets"
   git push
   ```
3. GitHub Pages يحدّث الموقع المنشور تلقائياً خلال دقيقة أو دقيقتين.

---

## الخطوة 7 — أي تحديث لاحق على الكود

في كل مرة تُعدّل فيها أي ملف (خصوصاً CSS أو JS):

1. **ارفع رقم `CACHE_VERSION`** في `js/config.js` (مثال: `"1.0.2"` → `"1.0.3"`) — هذه الخطوة ضرورية ليحصل المستخدمون الحاليون على النسخة الجديدة فوراً بدل نسخة Service Worker المخبأة القديمة.
2. ثم:
   ```bash
   git add .
   git commit -m "وصف التحديث"
   git push
   ```
3. انتظر دقيقة أو دقيقتين حتى يُنشر GitHub Pages التحديث تلقائياً.

---

## استكشاف الأخطاء الشائعة

| المشكلة | الحل |
|---|---|
| الصفحة تظهر بيضاء أو مسارات مكسورة على الرابط المنشور | تأكد أن كل روابط `<link>`/`<script>` نسبية (لا تبدأ بـ `/`) — كلها كذلك بالفعل في هذا المشروع. |
| Service Worker لا يُسجَّل | يعمل فقط عبر `https://` (أو `localhost`)؛ GitHub Pages يوفر `https` تلقائياً فلا مشكلة بعد النشر. |
| التحديثات لا تظهر للمستخدمين | تأكد أنك رفعت رقم `CACHE_VERSION` في `js/config.js` مع كل تعديل على الملفات الثابتة. |
| مزامنة Google Sheets تفشل برسالة خطأ | تأكد أن نوع النشر في Apps Script هو **Web app** وأن **Who has access** مضبوط على **Anyone**. |
