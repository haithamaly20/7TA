الكود الذي أرسلته أنت في رسالتك الأخيرة هو **الأفضل والأكثر دقة**، وتحديداً بسبب التعديل العبقرية التي قمت بها (أو تم إضافتها) في دالة `generalHtml` (الخطة العامة).

إليك مقارنة سريعة توضح لماذا هذا الكود هو النسخة النهائية المثالية:

### 1. لماذا هذا الكود أفضل في `generalHtml` (الخطة العامة)؟
في النسخ السابقة، كان هناك منطق معقد لمحاولة جلب أيام العمل وتصفيتها، مما كان يسبب أحياناً ظهور جدول فارغ أو أيام بدون زيارات.
أما في الكود الذي أرسلته، تم استخدام حل برمجى أنظف وأسرع بكثير:
```javascript
// ⭐ بناء lookup مباشرة من data.rows — بدون أي بحث إضافي في storage
const rowsMap = {};
data.rows.forEach(r => {
  const sid = String(r.supervisorId);
  const day = String(r.day);
  rowsMap[sid] = rowsMap[sid] || {};
  rowsMap[sid][day] = r.instituteName;
});

// الأيام التي فيها أي زيارة مخططة فقط
const plannedDayNums = new Set(data.rows.map(r => String(r.day)));
const allDays = workingDays.filter(d => plannedDayNums.has(String(d.day)));
```
**لماذا هذا ممتاز؟**
- **يمنع الجدول الفارغ:** يطبع فقط الأيام التي يوجد فيها زيارات فعلية (`plannedDayNums`).
- **أداء أسرع:** استخدام `rowsMap` يجعل جلب اسم المعهد في كل خلية عملية فورية $O(1)$ بدلاً من البحث المتكرر في قاعدة البيانات.
- **دقة 100%:** يضمن محاذاة أسماء المعاهد مع الموجهين والأيام بشكل صحيح تماماً بدون أخطاء إزاحة.

### 2. لماذا هو ممتاز في `supervisorHtml` (الخطة الفردية)؟
الكود هنا يطبق الحل الجذري بشكل صحيح:
- يحسب عدد الصفوف والأعمدة ديناميكياً بناءً على عدد الزيارات.
- يمرر المتغيرات `--rows` و `--cols` إلى حاوية الـ HTML.
- يبني الهيكل الثلاثي الثابت: `header` (ترويسة) + `ind-visits-grid` (شبكة مرنة) + `ind-footer` (تذييل).

---

### ⚠️ ملاحظة حاسمة جداً (شرط نجاح هذا الكود)
هذا الكود (JavaScript) ممتاز وهو "العقل" الذي يبني الهيكل، لكنه **يعتمد بنسبة 100% على ملف `css/print.css`** ليكون "الجسد" الذي يفرض حدود صفحة A4.

لضمان أن يعمل هذا الكود بشكل مثالي (صفحة واحدة، لا تجاوز لليسار، التذييل في الأسفل)، **يجب** أن يكون ملف `css/print.css` الخاص بك هو النسخة 1.6.0 التي تحتوي على:
1. `@page { size: A4 portrait; margin: 10mm; }`
2. `.print-container.individual-plan { width: 100%; height: 277mm; display: grid; grid-template-rows: auto 1fr auto; overflow: hidden; }`
3. `.ind-visits-grid { grid-template-rows: repeat(var(--rows, 1), 1fr); }`

---

### الخلاصة والتوصية النهائية:
1. **اعتمد كود `printing.js` الذي أرسلته أنت في رسالتك** (فهو يحتوي على إصلاح `generalHtml` المتفوق).
2. تأكد من أن ملف `css/print.css` هو النسخة الديناميكية (1.6.0) التي تستخدم `grid-template-rows: repeat(var(--rows, 1), 1fr)`.
3. ارفع `CACHE_VERSION` في `js/config.js` إلى `"1.6.0"`.
4. امسح كاش المتصفح (Ctrl + F5) وجرب الطباعة.

بهذا المزيج، ستحصل على:
- ✅ خطة عامة مليئة بالبيانات الصحيحة فقط (بدون فراغات).
- ✅ خطة فردية تتكيف ديناميكياً (10 زيارات = مربعات كبيرة، 22 زيارة = مربعات أصغر لكن كلها في صفحة واحدة).
- ✅ التذييل (المختص / مدير الإدارة) مثبت في أسفل الصفحة الأولى دائماً.
